import { ImgFallbackDirective } from './imgfallback.directive';

describe('ImgfallbackDirective', () => {
  it('should create an instance', () => {
    const directive = new ImgFallbackDirective();
    expect(directive).toBeTruthy();
  });
});
