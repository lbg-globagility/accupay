import { ExpandableRowContainerDirective } from './expandable-row-container.directive';

describe('ExpandableRowContainerDirective', () => {
  it('should create an instance', () => {
    const directive = new ExpandableRowContainerDirective();
    expect(directive).toBeTruthy();
  });
});
