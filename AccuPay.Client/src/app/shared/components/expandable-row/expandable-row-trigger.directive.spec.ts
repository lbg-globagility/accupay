import { ExpandableRowTriggerDirective } from './expandable-row-trigger.directive';

describe('ExpandableRowTriggerDirective', () => {
  it('should create an instance', () => {
    const directive = new ExpandableRowTriggerDirective();
    expect(directive).toBeTruthy();
  });
});
